import React from 'react';
import { Box } from '@mui/material';

const ChainConfig: React.FC = () => {
  return (
    <Box sx={{ p: 2 }}>
      {/* Chain configuration UI will be implemented here */}
    </Box>
  );
};

export default ChainConfig; 